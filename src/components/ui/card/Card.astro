---
import { twJoin } from 'tailwind-merge';

type Props = {
  class?: string;
};

const { class: className } = Astro.props;
---

<div class={twJoin(className, 'group relative flex flex-col items-start')}>
  <slot />
</div>
